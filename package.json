{
  "name": "claude-code-web-test",
  "version": "1.0.0",
  "description": "A fully-featured, responsive Snake game built with vanilla HTML, CSS, and JavaScript.",
  "main": "test_adversarial.js",
  "scripts": {
    "dev": "vite",
    "dev:vite": "vite",
    "dev:parcel": "parcel snake.html",
    "dev:live": "live-server --port=8080 --host=localhost --open=/snake.html --wait=200 --no-css-inject .",
    "dev:browsersync": "browser-sync start --server --files \"*.html, *.js, *.css\" --port 3000 --ui-port 3001 --no-notify --open --startPath \"/snake.html\" --reload-delay 100 --reload-debounce 100",
    "dev:http": "http-server -p 8081 -o /snake.html",
    "build": "vite build",
    "preview": "vite preview",
    "auto-deploy": "node auto-deploy.js",
    "watch": "node auto-deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:50715/git/ericbuess/claude-code-web-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^4.0.3",
    "parcel": "^2.16.0",
    "vite": "^7.1.11"
  }
}
